# app

Own route and layout composition, loading/error boundaries and feature assembly. Keep business interactions inside feature modules.

The application shell and health route are implemented in `src/app/`. Add feature-specific code here only when needed.
