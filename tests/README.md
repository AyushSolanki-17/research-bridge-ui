# Verification

`tests/e2e/app.spec.ts` verifies the production app shell, document language, health response and missing-route behavior with Playwright Chromium. Run `npm run check`; see [runtime commands](../README.md).

Add feature interactions, evidence visibility, keyboard access and cancellation checks when research features land. Full accessibility and research-journey coverage are not implemented yet.
